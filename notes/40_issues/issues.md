# lightspeedc.com 課題一覧

これから解決することの記録

> 📅 作成: 2026-09-17 / 更新: 2026-09-23

## 1. 課題

状態は 未 / 着手 / 済 の3段階。片付いた課題は消さずにここへ残す。

### 公開・運用

### i260917-01 Pages のビルドが push で自動的に積まれない **未**

2026-09-17 に2回連続で確認した。`master` への通常の push だけでは GitHub Actions の run が積まれず、`gh api -X POST repos/OWNER/REPO/pages/builds` の手動リクエストで初めてビルドが走っている。

ローカルルール4「Pages のビルドが queued で止まることがある」とは別の事象。あちらは積まれたジョブが滞留する話で、こちらはそもそも積まれない話。

**原因を特定した。** ビルド履歴を古い順に並べたところ、2026-09-16 の履歴書き換え（force push）の前後で切り替わっていた。書き換え前の `8de9462` は自動でビルドされ、書き換え後の `9899a1f` ・ `87de007` はどちらも自動で積まれず、手動リクエストが必要だった。`gh api repos/OWNER/REPO/hooks` は空で、通常の webhook 経由ではない（legacy Pages ビルドは GitHub 内部の仕組みで動く）。

対処として、Pages のソース設定を同じ値で PUT し直し、内部連携の再同期を試みた（設定自体は変えていない）。**2026-09-17 の次の push で確認したが、効果はなかった。** push 直後120秒監視しても run は積まれず、Pages の最新ビルドは push 前のコミットを指したまま。手動リクエストで初めて反映した。3回連続（9/16・9/17・今回）で自動トリガーが働いていない

利用者が GitHub 側の設定で build type を一度 Actions に変更し、`master` / `/docs` の legacy へ戻す操作を行った。2026-09-18 の次の push で確認したが、**これも効果はなかった。** push 直後120秒監視しても run は積まれず、手動リクエストで初めて反映した。master 側への push は4回連続（9/16・9/17・9/17・9/18）で自動トリガーが働いていない。

利用者が Pages のソースを `develop` / `/docs` に変更した。2026-09-18 の develop push（Active Directory グループ資料の掲載）で確認したところ、**今度は自動でビルドが積まれた。** push 直後に run が `in_progress` で現れ、手動リクエストなしで反映している。`master` ブランチ側にだけ何かが残っていて、`develop` ブランチには影響していなかった、という見立てが成り立つ。

確認後、利用者が Pages のソースを `master` / `/docs` に戻した。2026-09-20 の master push（AI PC 調査プロジェクトの掲載）で確認したところ、**master 側も自動でビルドが積まれるようになっていた。** push 直後に run が `in_progress` で現れ、手動リクエストなしで反映している。

2026-09-20 時点でいったん「解決」としたが、**2026-09-21 の master push（Bun買収記事の掲載）で再発した。** push 直後150秒監視しても run は積まれず、手動リクエストで初めて反映した。develop への切り替えは恒久対策ではなく、一時的に直っただけだったことになる。

2026-09-23 の master push（目次バッジの border-radius 修正）でも確認したところ、自動でビルドが積まれた（push 直後、約90秒の監視で `in_progress` を確認）。

**判定方法そのものに疑いが見つかった。** 9/17・9/18・9/21 の「再発」判定は、いずれも push 後120〜150秒の監視で「積まれない」と判断し、そのあと手動リクエストしていた。手動リクエストで作られた run と、自動で積まれた run は Actions の一覧では見分けがつかない。9/21 に記録した run（`5d167a8`）は commit から約3分後に作成されており、150秒の監視を打ち切った直後の手動リクエストで作られた可能性が高い。つまり**もっと待てば自動で積まれていたかどうかを、これまでの記録では確認できていない**。この監視は AI が自己ペースで秒数を数えるループで行っており、実際には指定した間隔・回数どおりに待てていなかった疑いもある。

対策として、ローカルルールに「ビルド確認は実時間の sleep で待つ」を新設した。次回以降は単発の `sleep 300〜600`（実時間5〜10分）で待ってから run の有無を確認する。

2026-09-23 の次の master push（AGENTS.md 移行）で、新設した運用で確認した。push 09/23 09:23 → 実時間5分の `sleep` を挟んで確認 09/23 09:29、`gh run list` で該当 commit（`55a3d96`）の run が `success` になっていることを確認した。手動リクエストは不要だった。

- 状態: 未
- force push の前後で切り替わったことを確認した（済）
- Pages のソース設定を PUT し直す（済・効果なし）
- build type を Actions → legacy（master/docs）へ切り替え直す（済・効果なし）
- Pages のソースを develop / /docs に変更して確認する（済・そのときは自動で積まれた）
- Pages のソースを master / /docs に戻し、次の master push で再確認する（済・そのときは自動で積まれたが、次の push で再発と判定した記録がある。ただし判定時間が短く、確度に疑いあり）
- ビルド確認の監視時間を実時間5〜10分（sleep コマンド）に延ばす（済・ローカルルールに反映）
- 延ばした監視時間で再度「積まれない」ケースが起きるか確認する（済・2026-09-23 は自動で積まれた。1回だけでは判定できないため、状態はまだ「未」で継続観察する）
- 手動リクエストを公開手順に恒久的に組み込む（未）
- GitHub サポートへの問い合わせを検討する（未）

### サイト整理（旧 status.html「今後の課題」から移行）

### i260917-02 旧サンプル群の整理 **未**

`mui` / `mdl` / `react` / `mithril` / `web-app-intro` は2016年頃の内容のまま `docs/` に残っている。新しいトップページからはリンクしておらず、URL は生存・HTTPS で配信中。`index-old.html` からは辿れる。

### i260917-03 証明書の自動更新の確認 **未**

有効期限は 2026-11-25。GitHub Pages の Let's Encrypt 証明書は自動更新されるが、DNS 設定を変更した場合は再発行が必要になる。

2026-09-17 に実測。`state: approved`、有効期限は 2026-11-25 のまま変わっていない（DNS も変更していないため想定どおり）。次に確認が要るのは有効期限が近づいてから。

### i260917-04 AAAA レコードの追加 ✅ **済**

IPv6 用の AAAA レコードは未設定だった。`2606:50c0:8000::153` 〜 `8003::153` の4件を追加した。

2026-09-18 に設定。権威 DNS（`01.dnsv.jp`）・Google（8.8.8.8）・Cloudflare（1.1.1.1）のいずれでも4件とも反映を確認した。IPv6 経由の HTTPS アクセスも実測で 200。

### i260917-05 index.json の扱い **未**

旧トップページが読み込むリンク一覧。新しいトップページは静的にリンクを記述しているため参照していない。`index-old.html` が使っているので残してある。

2026-09-17 に確認。`docs/index-old.html` の Mithril コードが `m.request` で今も読み込んでいる。新しいトップページ（`docs/index.html`）からのリンクは無いが、URL は生存しており実測で 200。**i260917-02（旧サンプル群の整理）の決着に従属する**。`index-old.html` を残す間はこのファイルも残す
